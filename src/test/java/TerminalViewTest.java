public class TerminalViewTest {
    /*@Test
    public void simpleUsage() throws InterruptedException {
        GameCell[][] gameMap = new GameCell[2][3];
        gameMap[0][0] = new GameCell(GameMapCellType.wall, Collections.emptyList());
        gameMap[0][1] = new GameCell(GameMapCellType.wall, Collections.emptyList());
        gameMap[0][2] = new GameCell(GameMapCellType.tunnel, Collections.emptyList());
        gameMap[1][0] = new GameCell(GameMapCellType.empty, Collections.emptyList());
        gameMap[1][1] = new GameCell(GameMapCellType.room, Collections.emptyList());
        gameMap[1][2] = new GameCell(GameMapCellType.room,
                new ArrayList<GameObject>() {{
                    add(new Item());
                }});

        GameModel gameModel = new GameModel(gameMap);

        // should print:
        // ##-
        //  +*
        TerminalView terminalView = new TerminalView(gameModel);
        Thread.sleep(2000);

        gameMap[1][2].setGameObjectList(Collections.emptyList());
        // should print:
        // ##-
        //  ++
        terminalView.showChanges();
        Thread.sleep(2000);
    }*/
}